{
  "name": "nairobi-hospital-admitting-rights-registry",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Admitting Rights Registry — The Nairobi Hospital",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"vite\" \"node --watch server/index.js\"",
    "dev:web": "vite",
    "dev:api": "node server/index.js",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server/index.js",
    "migrate": "node server/migrate.js"
  },
  "//": "The build toolchain sits in dependencies, not devDependencies: the host builds with NODE_ENV=production, where npm ci skips devDependencies and `vite` would not be found.",
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@vitejs/plugin-react": "^5.1.0",
    "compression": "^1.8.0",
    "express": "^5.2.1",
    "pg": "^8.23.0",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "tailwindcss": "^4.3.3",
    "vite": "^8.3.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
